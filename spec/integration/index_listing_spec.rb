require 'spec_helper'

    # it "should find the signed in user" do
    #   get :index, :id => @user.id
    #   expect(response.body).to have_text "Willy"
    # end


    # # it "should find the signed in user" do
    # #   get :index, :id => @user.id
    #   # assigns(:user).should == @user
